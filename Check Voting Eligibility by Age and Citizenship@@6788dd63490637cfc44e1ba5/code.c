#include <stdio.h>


int main() {
    int age,no;
    scanf("%d",&age);
    if(age>=18 && no==1){
        printf("Eligible");
    }
    else{
        printf("Not Eligible");
    }
    return 0;
}