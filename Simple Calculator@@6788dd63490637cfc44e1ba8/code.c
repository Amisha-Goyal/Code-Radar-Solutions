#include <stdio.h>
int main(){
    int m,n,sum,diff,multiply,div;
    char ch;
    scanf("%d %d %c",&m,&n,&c);
    if(ch=='+'){
        sum=a+b;
        printf("%d",sum);
    }
    else if(ch=='-'){
        diff=a-b;
        printf("%d",diff);
    }
    else if(ch=='*'){
        multiply=a*b;
        printf("%d",myltiply);
    }
    else if(ch=='/'){
        div=m/n;
        printf("%d",div);
    }
    return 0;
}